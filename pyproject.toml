[project]
name = "deadspot-sentinel"
version = "0.4.1"
description = "Fast dual-Wi-Fi monitoring and assisted failover for NetworkManager Linux desktops"
requires-python = ">=3.11"
dependencies = ["PyQt6>=6.7,<7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
